# Copy to .env and fill in the values from Supabase > Connect.
# Run with: flutter run --dart-define-from-file=.env
# Both blank = sample-data preview. Restart/rebuild after changing values.
# Client-visible configuration only. Never use a secret or service-role key.
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
